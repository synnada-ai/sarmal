name: Pull Request Labeler
on:
- pull_request_target

jobs:
  labeler:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
    - name: Check out the repository
      uses: actions/checkout@v4
      with:
        ref: ${{ github.event.pull_request.head.ref }}
    - id: pr-labeler
      uses: actions/labeler@v5
      with:
        repo-token: "${{ secrets.GITHUB_TOKEN }}"
        sync-labels: true
  
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install PyYAML
      run: pip install pyyaml

    - name: Assign Reviewers
      if: steps.pr-labeler.outputs.all-labels
      run: |
        ls -la
        python .github/scripts/assign_reviewers.py "${{ steps.pr-labeler.outputs.all-labels }}"
        
    # - id: assign_people
    #   if: contains(steps.pr-labeler.outputs.all-labels, 'physical')
    #   run: |
    #     gh pr edit --add-assignee mehmetozsoy1
